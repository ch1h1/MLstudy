# 全ノートブック（01〜05）コード解説

---

## 全体の流れ（パイプライン概要）

```
01_two_stage_clustering   → テレビ番組シーンデータを月次×分類別にトピッククラスタリング＋年間統合
         ↓ 出力: mdata_with_topics.parquet（トピック付きMデータ）
02_data_marge_mxi         → インテージ視聴ログ（個人単位）× Mデータ（番組シーン単位）を時刻重複で結合
         ↓ 出力: merged_YYYYMM.parquet（個人×シーン結合データ）
03_mdata_topic_analysis   → Mデータ単体で放送局間のトピック構成差異をJSD・エントロピーで分析
         ↓ 出力: topic_ref_distributions.pkl（月×分類別の参照分布）
04_mxi_user_analysis      → 個人別のJSD（視聴偏り）を算出し、デモグラ属性との関係を多角分析
05_mxi_negindex           → ネガティブ情報直後のチャンネル変更行動（回避行動）をデモグラ別に分析
```

**データの関係**:
- **Mデータ**: テレビ番組のシーン単位メタデータ（ヘッドライン、分類、放送局、放送時間等）
- **インテージデータ**: 個人モニターの分単位テレビ視聴ログ（誰がいつどの局を見たか）
- **デモグラ**: インテージモニターの属性情報（年齢、性別、職業、学歴等）

---

# 01_two_stage_clustering.ipynb

## 目的
月次×分類別にBERTopicでクラスタリングした細かいトピック群を、年間を通して「同じ話題」として追跡できるようにする。

---

### Cell 0（Markdown）— タイトルと処理概要
二段階クラスタリングの3ステップを宣言:
- Step1: 月次クラスタの代表ベクトル（embedding平均）を抽出
- Step2: 代表ベクトルを年間でAgglomerativeClusteringにかける
- Step3: 結果整理・可視化

---

### Cell 1（Code）— ライブラリ読込・設定

**挙動**: 必要なライブラリの読込と、全体で使う定数の定義。

```python
EMBEDDING_MODEL_NAME = "cl-nagoya/ruri-large"
```
- 日本語テキストをベクトル化するためのモデル。名古屋大が公開している日本語特化の文埋め込みモデル。

```python
DISTANCE_THRESHOLDS = {
    '社会': 0.1,
    '政治・国際': 0.1,
    '暮らし': 0.03,
}
```
- Step2の年間クラスタリングで使うコサイン距離の閾値。小さいほど細かく分かれる。
- **具体例**: 閾値0.1なら、コサイン距離0.1以下のトピック同士が同じ年間トピックに統合される。
- **反例**: 閾値を0.01にすると、ほぼ同一のトピックしか統合されず、年間追跡の意味が薄くなる。

```python
TARGET_CATEGORIES = ['政治・国際', '社会']
```
- 分析対象のニュース分類。ドラマやバラエティは除外。

---

### Cell 3（Code）— データ読み込み

**挙動**: `DATA_PATH`配下のCSVファイルを全部読み込んで1つのDataFrameに結合。

```python
files = sorted(glob.glob(os.path.join(DATA_PATH, "*.csv")))
df = pd.concat([pd.read_csv(f, encoding='cp932') for f in files], ignore_index=True)
```
- `cp932`（Shift_JIS拡張）でCSV読込 → Mデータが日本語Windows環境で出力されたもの。

```python
df['番組開始年月'] = pd.to_datetime(df['番組開始日時24']).dt.to_period('M').astype(str)
```
- 「番組開始日時24」列からYYYY-MM形式の年月を抽出。「24」は24時間制の意味（深夜番組は翌日扱いにならない）。

---

### Cell 4（Code）— データクリーニング

**挙動**: 対象分類のみ抽出→シーン放送時間を秒数変換→メモ・ヘッドラインのテキストクリーニング→結合テキスト生成。

#### `parse_airtime(val)` 関数
```python
def parse_airtime(val):
    """'0 days 00:06:26' のような文字列を秒数に変換"""
    td = pd.to_timedelta(val)
    return td.total_seconds()
```
- **具体例**: `'0 days 00:06:26'` → `386.0`（秒）
- **反例**: `None`や不正な文字列 → `0.0`を返す（tryで握り潰し）

#### メモのクリーニング — `clean_memo(text)` 関数

2種類のタグ処理:

1. **全削除（`REMOVE_TAGS`）**: `【出演】〇〇アナウンサー` → 丸ごと削除。出演者名やBGM情報はトピック分析に不要。
2. **タグ名のみ除去（`STRIP_TAG_ONLY`）**: `【施設】東京スカイツリー` → `東京スカイツリー`（施設名自体はトピックに関係するため残す）

```python
REMOVE_TAGS = {'出演', '声の出演', '告知', '資料・協力', ...}
STRIP_TAG_ONLY = {'施設', '商品', '人物', 'コメント', ...}
```

さらに全角・半角数字を除去。トピック分類に数字は邪魔になるため。

#### ヘッドラインのクリーニング
```python
df_target['ヘッドライン_clean'] = df_target['ヘッドライン'].str.replace(r'＜[^＞]*＞', '', regex=True)
```
- `＜ＮＮＮ・ＮＥＷＳＺＩＰ！＞` のような番組名表記を除去。

#### 結合テキスト
```python
df_target['text_combined'] = (ヘッドライン_clean + '。' + メモ_clean)
```
- ヘッドラインとメモを結合して1つのテキストにする。これがembeddingの入力になる。

---

### Cell 6-7（Code）— Embedding生成

```python
model = SentenceTransformer(EMBEDDING_MODEL_NAME)
```
- `ruri-large`モデルを読み込み（1024次元のベクトルを生成）。

```python
if 'ruri' in EMBEDDING_MODEL_NAME.lower():
    texts = ['文章: ' + t for t in texts]
```
- ruriモデルは入力にプレフィックス`文章: `をつける仕様。これがないと性能が落ちる。
- **反例**: e5系モデルなら`passage: `を使う。プレフィックスなしだと精度が低下する。

```python
embeddings = model.encode(texts, show_progress_bar=True, batch_size=256)
```
- 全テキストを一括でベクトル化。shape = (行数, 1024)。

---

### Cell 9（Code）— Step1.5: 月次×分類別 BERTopicクラスタリング

**挙動**: 分類×月ごとにBERTopicを実行し、各シーンにトピックIDを割り当てる。

```python
from bertopic import BERTopic
from sklearn.cluster import HDBSCAN
```
- BERTopicはHDBSCANベースのトピックモデル。embeddingを入力にクラスタリングする。

#### `japanese_tokenizer(text)` 関数 — 日本語トークナイザ

**挙動**: fugashi（MeCab互換）で形態素解析し、名詞連続を複合語として結合＋動詞も抽出。

```python
_tagger = Tagger()  # fugashiのMeCab互換タガー
```

処理の流れ:
1. テキストを形態素解析
2. 名詞が連続したら結合バッファに蓄積（例: `千葉` + `大学` + `医学部` → `千葉大学医学部`）
3. 名詞以外が来たらバッファをフラッシュ（2文字以上なら採用）
4. 動詞は`STOP_VERBS`にないものだけ残す

**具体例**:
- `"イスラエル軍がガザ地区への地上侵攻を開始した"` → `['イスラエル軍', 'ガザ地区', '地上侵攻', '開始']`
- `"千葉大学医学部附属病院の肝胆膵外科に所属"` → `['千葉大学医学部附属病院', '肝胆膵外科', '所属']`

**`STOP_VERBS`の意味**: `'イル'`, `'スル'`, `'ナル'` など、どんな文脈にも出る汎用動詞をストップワードとして除外。これらがあるとトピックの弁別力が落ちる。
- **反例**: `STOP_VERBS`を空にすると、「する」「なる」がどのトピックにも出現し、代表キーワードが意味をなさなくなる。

月次×分類のループ内でBERTopicを実行し、`topic`列（トピックID）と`topic_prob`列（所属確率）を付与。`topic=-1`はアウトライア（どのトピックにも属さない）。

---

### Cell 10（Code）— 代表ベクトル（重心）の算出

**挙動**: 分類×月×topicごとに、所属するシーンのembedding平均（重心）を計算。

```python
centroid = group_embs.mean(axis=0)  # embedding平均
```

- `topic=-1`（アウトライア）は除外。
- クラスタ内分散（`centroid_variance`）も計算。重心からの平均コサイン距離で測る。

**具体例**: 「ガザ侵攻」トピックに50シーンが属していたら、その50個のembeddingの平均ベクトルが代表ベクトル。

---

### Cell 12（Code）— Step2: 年間クラスタリング

**挙動**: 分類ごとに、月次トピックの代表ベクトル同士をAgglomerativeClusteringで統合。

#### 距離行列の調整
```python
base_dist = cosine_distances(X)  # コサイン距離（0〜2）
adjusted_dist = base_dist * (1 - alpha * var_norm)
```
- **分散が大きいクラスタ同士は統合しやすくする**（距離を縮小）。
- `alpha=0.1`で効き具合を調整。
- **理由**: 分散が大きい＝テーマが広い＝他の関連トピックと統合されやすいはず。

#### クラスタリング実行
```python
threshold = 0.08  # 最終的に採用した閾値
final_labels = AgglomerativeClustering(
    n_clusters=None,
    distance_threshold=threshold,
    metric='precomputed',
    linkage='average',
).fit_predict(adjusted_dist)
```
- 距離閾値ベースの凝集型クラスタリング。閾値以下の距離のクラスタを順次統合。
- `linkage='average'`: クラスタ間距離はメンバー間距離の平均。
- **具体例**: 「ガザ侵攻」の8月トピックと9月トピックの代表ベクトル距離が0.05なら統合される。

閾値0.02〜0.20を探索してエルボー法で適切な閾値を確認するコードも含まれている。

---

### Cell 15-16（Code）— Step3: 結果整理

CSVに出力し、年間トピックごとにキーワードを集約。

#### `aggregate_keywords(group)` 関数
```python
parts = str(words_str).split('_')  # 'topicID_word1_word2_...'
```
- BERTopicが出力した代表キーワード文字列から、topicIDを除いてキーワード部分を抽出。
- 全月のキーワードをカウントして頻出上位10個を返す。

---

### Cell 17（Code）— 月別推移の可視化

`plot_trends()` 関数で、年間トピックごとの月別シーン数・放送時間を折れ線グラフ化。規模上位20トピックのみ表示。

---

### Cell 18（Code）— dfへのマージバック

年間トピックID（`annual_topic_id`）を元のdf全行に紐づけ。月次topic経由でマージ。

---

### Cell 20-21（Code）— ネガティブフラグ付与

**挙動**: ヘッドラインにネガティブキーワードが含まれるかで`is_negative`列を付与。

```python
NEG_KEYWORDS = {
    "事件・犯罪": ["殺人", "刺殺", "強盗", ...],
    "薬物": ["薬物", "覚醒剤", "大麻"],
    ...
}
EXCLUDE_WORDS = ["防ぐ", "守る", "対策", "防止", "防災", "訓練", "爆発的"]
```
- **具体例**: `"ガザ地区で空爆 死者多数"` → 「空爆」「死者」でヒット → `is_negative=1`
- **反例**: `"防災訓練で爆発的な人気"` → 「爆発的」「訓練」で除外 → `is_negative=0`
- 対象は「政治・国際」「社会」のみ（ドラマの「殺人」はカウントしない）

---

### Cell 22（Code）— Parquet保存

最終的なdfを`mdata_with_topics.parquet`として保存。snappy圧縮。以降のノートブックはこのファイルを入力として使用。

---
---

# 02_data_marge_mxi.ipynb

## 目的
Mデータ（番組シーン単位）× インテージ視聴ログ（個人×分単位）を時刻と放送局で結合し、「誰がどのシーンを見たか」を特定する。

---

### Cell 1（Code）— 設定

```python
CHANNEL_MAP = {
    'NHK': 'NHK', 'ETV': '教育', 'NTV': 'NTV',
    'TBS': 'TBS', 'CX': 'CX', 'EX': 'EX', 'TX': 'TX',
}
```
- インテージの放送局コード（`channel_2`）をMデータの放送局名に変換するマッピング。
- **反例**: マッピングにない局（BSなど）はdropnaで除外される。

```python
DROP_COLS = {'household_id', 'watch_flg', 'program_start_date', ...}
```
- 不要カラムを除外してメモリ節約。

---

### Cell 3（Code）— Mデータ読み込み・シーンテーブル準備

```python
df_scene = pd.read_parquet(MDATA_PATH, columns=MDATA_COLS)
```
- 01で保存したparquetファイルを読込。必要列のみ。

```python
df_scene = df_scene[df_scene['シーン開始dt'] != df_scene['シーン終了dt']]
```
- 0秒シーン（開始＝終了）を除外。これらは結合時に意味がないノイズ。

---

### Cell 5（Code）— 月次ループ：インテージ読込→結合→保存

**挙動**: 月ごとにインテージCSVを読み込み、Mデータと放送局＋時刻重複で結合してparquetに保存。

#### Phase 1: CSV読込→放送局別に一時parquetへ振り分け

```python
table = pa_csv.read_csv(f)  # PyArrow CSVで高速読み込み
df_tmp['放送局'] = df_tmp['channel_2'].map(CHANNEL_MAP)
```
- PyArrowのCSVリーダーでpandasより高速に読み込み。
- 放送局マッピング→マッピング外を除外→放送局別に一時parquetに追記保存。
- **理由**: 全データを一度にメモリに載せるとOOMになるため、放送局別に分割処理。

#### Phase 2: 放送局別にMデータとソート済みマージ

インテージの各行（ある人の、ある分の視聴記録）に対し、Mデータのシーンテーブルと「同じ放送局で、時刻が重なっている」条件で結合する。

具体的には、インテージの`watch_date`がシーンの`シーン開始dt`〜`シーン終了dt`の範囲内にあるシーンを紐づける（`merge_asof`やソート済みマージで実現）。

出力: `merged_YYYYMM.parquet`（月ごと）。各行は「モニターID × 視聴日時 × 該当シーンのメタ情報」。

---
---

# 03_mdata_topic_analysis.ipynb

## 目的
放送局間でトピック構成がどれだけ異なるかをJensen-Shannon Divergence（JSD）とエントロピーで定量化。放送局の「報道の多様性」を測る。

---

### Cell 0（Code）— JSD・エントロピー計算

#### `compute_jsd_entropy(data, top_n=20)` 関数

**挙動**: 月×分類×放送局ごとに、トピックの放送時間構成比を算出し、全局平均との乖離（JSD）と分布の均等さ（エントロピー）を計算。

処理の流れ:
1. 月×分類でグループ化
2. トピック別の放送秒合計を集計
3. 上位`top_n`トピック＋それ以外を「-1（その他）」にまとめる
4. 放送局×トピックのピボットテーブル作成
5. 各局の構成比（`dist`）と全局平均（`mean_dist`）を算出
6. 各局とmean_distのJSD（二乗）を計算

```python
"JSD": jensenshannon(p, mean_dist) ** 2,
"エントロピー": entropy(p, base=2),
```

- **JSD**: 0に近いほど全局平均と似ている。高いほど「独自の報道傾向」がある。
  - **具体例**: NHKが災害報道に偏っていれば、他局平均との乖離でJSDが高くなる。
  - **反例**: 全局が同じトピック構成で報道していればJSD≈0。
- **エントロピー**: 高いほど多様なトピックを均等に報道。低いほど少数トピックに集中。

#### `plot_jsd_entropy()` 関数
月別×分類別に散布図を描画。X軸=エントロピー、Y軸=JSD。全局平均の十字線と月・分類ごとの中央値（点線）も表示。

---

### Cell 1（Code）— topic_prob フィルタの影響確認

`topic_prob >= 0.8`でフィルタした場合の残存率をトピック別に可視化。BERTopicの確信度が低いシーンをどれだけ除外するかの検討用。

---

### Cell 2-3（Code）— 全体＋ジャンル別のJSD・エントロピー分析

全番組ジャンルまとめてと、「ニュース/報道」「情報/ワイドショー」別に分けて分析。

---

### Cell 4（Code）— 参照分布の保存

```python
ref_data[(month, bunrui)] = {
    "top_n": top,        # 上位Nトピックのリスト
    "labels": all_labels, # 上位N + [-1]
    "mean_dist": mean_dist, # 全局平均の構成比ベクトル
}
```
- **この辞書が04のノートブックで使われる**。個人の視聴トピック構成と放送局平均を比較してJSDを算出するための参照データ。
- 保存先: `topic_ref_distributions.pkl`

---
---

# 04_mxi_user_analysis.ipynb

## 目的
個人別のトピック視聴構成の偏り（JSD）を算出し、デモグラ属性（年齢、性別、職業、学歴等）との関連を多角的に分析する。

---

### Cell 0（Code）— 設定・共通描画関数

```python
USE_COLS = ["monitor_id", "放送局", "watch_date", "番組ジャンル", "分類",
            "topic", "topic_prob", "annual_topic_id"]
```

#### `plot_jsd_scatter()` 関数
**定義場所**: Cell 0
- 分類×デモグラグループ別に「視聴時間 vs JSD」の散布図を描画。
- グループ以外のデータは灰色で薄く背景表示。対象グループは色付き。
- **具体例**: 年代別なら、「20代」「30代」…がそれぞれ別パネルで表示される。

#### `plot_quantile_scatter()` 関数
**定義場所**: Cell 0
- 指標の上位5%/下位5%を色分けした散布図。
- **具体例**: CH変更回数の上位5%（ザッピング多い人）が赤で強調表示される。

---

### Cell 1（Code）— 個人別JSD算出

**挙動**: 03で保存した参照分布（`topic_ref_distributions.pkl`）を読み込み、個人の視聴トピック構成と全局平均のJSDを計算。

```python
ref = ref_data[key]     # Cell 4 of 03 で保存した辞書
mean_dist = ref["mean_dist"]  # 全放送局平均のトピック構成比
```

処理の流れ:
1. 分類ごとにループ
2. 個人×トピックのクロス集計（`pd.crosstab`）
3. 行合計で割って個人の構成比ベクトル化
4. 全局平均との **JSD** を計算（KLダイバージェンスの対称版）

```python
m = 0.5 * (user_dist + mean_dist)
kl_pm = nansum(user_dist * log2(user_dist / m))
kl_qm = nansum(mean_dist * log2(mean_dist / m))
jsd_vals = 0.5 * (kl_pm + kl_qm)
```

- **JSDが高い人**: 全局平均と異なるトピック配分で視聴している＝特定トピックに偏っている。
  - **具体例**: 政治報道ばかり見る人はJSDが高い。
  - **反例**: まんべんなく色々なトピックを見ている人はJSD≈0に近い。

---

### Cell 2（Code）— デモグラ紐づけ

```python
demo_month = demo[(demo["attr_start_ymd"] <= month_end) & (demo["attr_end_ymd"] >= month_start)]
```
- モニターの属性情報は期間付き。該当月に有効な属性レコードを取得。

---

### Cell 4（Code）— 年齢・性年代別の散布図

`psk_003`（年代）、`psk_007`（性年代）別に視聴時間×JSDを可視化。

---

### Cell 6（Code）— Kruskal-Wallis検定によるスクリーニング

**挙動**: 全psk属性に対して、JSD・視聴時間の群間差をKruskal-Wallis検定で測定。

```python
h_jsd, p_jsd = kruskal(*jsd_groups)
eta2_jsd = (h_jsd - k + 1) / (n_total - k)  # 効果量η²
```
- **Kruskal-Wallis検定**: ノンパラメトリックな多群比較。分布の正規性を仮定しない。
- **η²（イータ二乗）**: 効果量。0に近いほど群間差がない、1に近いほど大きい。
  - **具体例**: η²=0.05なら「属性で5%の分散を説明できる」。
  - **反例**: η²=0.001なら統計的に有意でも実質的な意味はほぼない。

結果をη²降順でソートして、JSD/視聴時間に最も影響する属性を特定。

---

### Cell 8（Code）— 視聴行動指標の算出

3つの行動指標を算出:

1. **CH変更回数**（`avg_ch_change`）: 日別のチャンネル変更回数の平均
2. **平日1日平均視聴時間**（`avg_weekday_min`）
3. **休日1日平均視聴時間**（`avg_holiday_min`）

CH変更の計算:
```python
ch_change = (放送局 != 前行の放送局) | (モニター変更) | (日付変更)
daily_ch = ch_change.groupby([monitor_id, date]).sum() - 1  # 最初の行は変更ではない
```

---

### Cell 11-12（Code）— 職業別クロス集計

`psk_012`（職業）別のJSD分布を箱ひげ図で可視化。さらに職業×年代のヒートマップで交互作用を確認。

---

### Cell 14（Code）— 最終学歴×年代ヒートマップ

雇用形態（正社員、専業主婦、パート）別にさらに分割。JSD中央値をヒートマップで表示。

---

### Cell 16（Code）— CH変更回数 上位/下位10%の分析

```python
ch_upper = user_jsd_ch_demo["avg_ch_change"].quantile(0.90)
```
- ザッピングが多い人/少ない人それぞれでデモグラ属性とJSDの関係を分析。

---

### Cell 18-19（Code）— CH変更回数×年代の交互作用

年代×CH変更回数（四分位）でJSD中央値のヒートマップを作成。「若い×ザッピング多い」と「高齢×ザッピング少ない」でJSDの差が異なるかを検証。

---

### Cell 20（Code）— 分類別CH→JSD関係

CH変更回数を十分位に分けてJSD中央値の推移を折れ線グラフ化。分類（政治・国際 vs 社会）で傾向が異なるかをヒートマップでも確認。

---
---

# 05_mxi_negindex.ipynb

## 目的
ネガティブ情報（事件・犯罪・災害等）を視聴した直後のチャンネル変更行動（ネガティブ回避行動）を分析する。

---

### Cell 1（Code）— データ読込

```python
USE_COLS = ["monitor_id", "放送局", "watch_date", "番組ジャンル", "分類", "is_negative"]
```
- `is_negative`は01で付与したフラグ。

---

### Cell 3（Code）— CH変更イベントの検出

**挙動**: CM行を除外した上で、「次の番組本編行」との比較でCH変更を判定。

```python
intage_nocm = intage[intage["分類"] != "CM"].copy()
```
- CM行を除外する理由: CM中の局変更はザッピングではなく、CMまたぎの正常な視聴パターン。

```python
same_monitor = intage_nocm["monitor_id"] == intage_nocm["monitor_id"].shift(-1)
time_diff = (next_watch - intage_nocm["watch_date"]).dt.total_seconds()
next_station_diff = intage_nocm["放送局"] != intage_nocm["放送局"].shift(-1)
```

**CH変更フラグの定義** （`ch_change_3min`）:
- 3分以内に別局を視聴 → CH変更
- 3分超の空白 → テレビを消した（これもCH変更扱い）

**具体例**:
- 15:00にNHKを見て、15:02にTBSを見る → 3分以内＋別局 → `ch_change_3min=1`
- 15:00にNHKを見て、18:00にまたNHKを見る → 3分超 → `ch_change_3min=1`（テレビ消した）
- 15:00にNHKを見て、15:01にNHKを見る → 3分以内＋同局 → `ch_change_3min=0`

```python
intage_nocm["neg_then_change"] = (is_negative==1) & (ch_change_3min==1)
```
- ネガティブシーンの直後にCH変更したかどうかのフラグ。

---

### Cell 5（Code）— 基本統計

**挙動**: ネガティブ視聴分 vs 非ネガティブ視聴分のCH変更率を比較。

```python
neg_ch_rate = neg_rows["ch_change_3min"].mean()
nonneg_ch_rate = nonneg_rows["ch_change_3min"].mean()
```

- 比率（ネガ/非ネガ）が1より大きければ、ネガティブ情報の後にCH変更する傾向がある。
- **具体例**: ネガCH変更率5%, 非ネガCH変更率3% → 比率1.67 → ネガの方が67%多くCH変更する
- **反例**: 比率≈1.0なら、ネガティブかどうかはCH変更に影響しない

---

### Cell 7（Code）— モニター別のネガティブ回避傾向

```python
monitor_avoidance["avoidance_diff"] = neg_ch_rate - nonneg_ch_rate  # 差
monitor_avoidance["avoidance_ratio"] = neg_ch_rate / nonneg_ch_rate  # 比
```

- `avoidance_diff > 0`: ネガティブ時の方がCH変更率が高い（回避傾向）
- `avoidance_diff < 0`: ネガティブ時の方がCH変更率が低い（留まる傾向）

`MIN_NEG_MIN = 3`: ネガティブ視聴が3分未満のモニターは除外（サンプルが少なすぎて不安定なため）。

ヒストグラムと「非ネガCH変更率 vs ネガCH変更率」の散布図（y=x線が差なし基準）で可視化。

---

### Cell 9（Code）— デモグラ属性との紐づけ＋Kruskal-Wallis

`avoidance_diff`に対してKruskal-Wallis検定を実施。η²が大きい属性＝ネガティブ回避傾向に個人差を生むデモグラ要因。

箱ひげ図でη²上位3属性を可視化。

---

### Cell 10（Code）— ザッピング上位10%層の分析

```python
ch_p90 = monitor_total_ch["total_ch_rate"].quantile(0.90)
zap_top10 = monitor_total_ch[monitor_total_ch["total_ch_rate"] >= ch_p90].index
```
- 全体のCH変更率上位10%（＝よくザッピングする人）に絞って再分析。
- **目的**: ザッピングが多い人はネガティブ回避傾向がより顕著か？

全体との比較棒グラフと、回避差分のヒストグラム比較を表示。

---

### Cell 12（Code）— 補足：CH変更しない視聴者の分析

```python
new_session = ~(same_monitor & same_date & (time_diff <= 180))
df_sess["session_id"] = new_session.cumsum()
```
- **セッション定義**: 同一モニター・同一日で、3分以上の空きがあれば別セッション。
- セッション長別にCH変更なし率を集計。

**分析内容**:
- 30分以上視聴しても1つの局から動かないセッションはどれくらいあるか
- CH変更なしの長時間セッションで見ているジャンルは何か（ドラマ？ニュース？）
- CH変更ありの場合と比較して構成比がどう違うか

---

## 関数・変数の相互参照まとめ

| 定義場所 | 名前 | 参照先 |
|---------|------|-------|
| 01 Cell 1 | `EMBEDDING_MODEL_NAME` | 01 Cell 6, 7 |
| 01 Cell 4 | `clean_memo()` | 01 Cell 4 内でのみ使用 |
| 01 Cell 4 | `parse_airtime()` | 01 Cell 4 内でのみ使用 |
| 01 Cell 9 | `japanese_tokenizer()` | 01 Cell 9 内のBERTopic vectorizer |
| 01 Cell 9 | `STOP_VERBS` | 01 Cell 9 `japanese_tokenizer()` 内 |
| 01 Cell 16 | `aggregate_keywords()` | 01 Cell 16 内でのみ使用 |
| 01 Cell 17 | `plot_trends()` | 01 Cell 17 内でのみ使用 |
| 01 Cell 20-21 | `NEG_KEYWORDS`, `EXCLUDE_WORDS` | 01 Cell 20-21（重複定義） |
| 01 Cell 22 | 出力: `mdata_with_topics.parquet` | **02 Cell 3** で読み込み |
| 02 Cell 1 | `CHANNEL_MAP` | 02 Cell 5 |
| 03 Cell 0 | `compute_jsd_entropy()` | 03 Cell 2, 3 |
| 03 Cell 0 | `plot_jsd_entropy()` | 03 Cell 2, 3 |
| 03 Cell 4 | 出力: `topic_ref_distributions.pkl` | **04 Cell 1** で読み込み |
| 04 Cell 0 | `plot_jsd_scatter()` | 04 Cell 4, 14等 |
| 04 Cell 0 | `plot_quantile_scatter()` | 04 Cell 9 |
| 04 Cell 1 | `user_jsd`（個人別JSD） | 04 Cell 2以降全体 |
| 04 Cell 8 | `avg_ch_change` | 04 Cell 9, 16, 18, 19, 20 |
| 05 Cell 3 | `intage_nocm`（CM除外済みデータ） | 05 Cell 5, 7, 12 |
| 05 Cell 7 | `monitor_av_filtered` | 05 Cell 9, 10 |
